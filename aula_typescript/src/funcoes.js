"use strict";
function calculaArea(base, altura) {
    return base * altura;
}
//ARROW Function
const calculaArea2 = (base, altura) => base * altura;
//REST
function somar(...numeros) {
    // numeros.reduce()
    console.log(numeros);
}
//VOID:quando não temos return, o return é o void
function idade(...numeros) {
    console.log(idade);
}
function test() {
    if (10 > 5) {
        return 'dez é maior que cinco';
    }
    else {
        return 5;
    }
}
const resultadoDoTeste = test();
function mutltiplicacao(numero1, numero2) {
    console.log(mutltiplicacao);
}
;
